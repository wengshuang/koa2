const member = require('./member')
const user = require('./user')
const tags = require('./tags')
const blogs = require('./blogs')
const browserIp = require('./browser-ip')
module.exports = {
  member,
  user,
  tags,
  blogs,
  browserIp
}
